# render.yaml — Render Infrastructure-as-Code
# This file tells Render how to deploy all your services automatically.
# Docs: https://render.com/docs/blueprint-spec

services:
  # ─── Django Backend ────────────────────────────────────────────────────
  - type: web
    name: taskflow-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /
    envVars:
      - key: SECRET_KEY
        generateValue: true          # Render auto-generates a secure random value
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "taskflow-backend.onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: taskflow-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: taskflow-redis
          type: redis
          property: connectionString
      - key: CORS_ALLOWED_ORIGINS
        value: "https://taskflow-frontend.onrender.com"
      # Email — fill these in Render dashboard
      - key: EMAIL_BACKEND
        value: "django.core.mail.backends.smtp.EmailBackend"
      - key: EMAIL_HOST
        value: "smtp-relay.brevo.com"
      - key: EMAIL_PORT
        value: "587"
      - key: EMAIL_USE_TLS
        value: "True"
      - key: EMAIL_HOST_USER
        sync: false                  # Set manually in Render dashboard
      - key: EMAIL_HOST_PASSWORD
        sync: false
      - key: DEFAULT_FROM_EMAIL
        value: "TaskFlow <noreply@taskflow.app>"
      # OAuth — fill these in Render dashboard
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GITHUB_CLIENT_ID
        sync: false
      - key: GITHUB_CLIENT_SECRET
        sync: false

  # ─── React Frontend (Static Site) ─────────────────────────────────────
  - type: web
    name: taskflow-frontend
    runtime: docker
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    plan: free
    buildArgs:
      - key: VITE_API_URL
        value: "https://taskflow-backend.onrender.com/api"
      - key: VITE_GOOGLE_CLIENT_ID
        sync: false                  # Set in Render dashboard
      - key: VITE_GITHUB_CLIENT_ID
        sync: false

  # ─── Redis ────────────────────────────────────────────────────────────
  - type: redis
    name: taskflow-redis
    plan: free
    ipAllowList: []                  # Only allow internal Render connections

databases:
  # ─── PostgreSQL ───────────────────────────────────────────────────────
  - name: taskflow-db
    plan: free
    databaseName: taskflow_db
    user: taskflow_user
